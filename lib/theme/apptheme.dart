import 'package:flutter/material.dart';

class AppTheme {
  static const primaryColor = Colors.black38;
  static const primaryShadow = <Shadow>[
                                  Shadow(
                                    offset: Offset(2.0, 2.0),
                                    blurRadius: 3.0,
                                    color: Colors.black38,
                                  ) 
                                ];
}